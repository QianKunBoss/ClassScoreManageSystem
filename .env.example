# CSMS 生产环境配置
# 复制此文件为 .env 并修改以下配置

# 运行时会话密钥【必填，必须修改】
# 生成方式：openssl rand -hex 32   或   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 要求：至少 32 字符的随机值。生产环境如果未设置、长度不足或仍为下方示例值，
#      服务会在启动阶段直接拒绝启动（见 server/plugins/00.security-guard.ts）。
# 说明：
#  - 该密钥用于加密签名会话 Cookie，泄露或可猜测将导致管理员会话可被离线伪造。
#  - NUXT_SESSION_PASSWORD 为运行期变量，任何部署方式（含预构建产物 / npm run preview / PM2 / Docker）均实时生效；
#  - SESSION_SECRET 仅在「npm run build 之前」设置才被烘焙进产物，运行期修改无效。
NUXT_SESSION_PASSWORD=change-this-to-a-random-secret

# 时区
TZ=Asia/Shanghai
